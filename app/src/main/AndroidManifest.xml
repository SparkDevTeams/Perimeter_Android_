<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sparkdev.perimeter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      <activity android:name=".activities.Login.LoginActivity">
      </activity>

      <activity android:name=".activities.Inbox.InboxActivity">

      </activity>

      <activity android:name=".activities.MessageThread.MessageThread">
      </activity>

      <activity android:name=".activities.Settings.Settings_Activity">
      </activity>

      <activity android:name=".activities.Settings.MainActivity">

      </activity>
        <activity android:name=".activities.SignUp.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>